const filter = require('./ex5');

/**
 * @param {Array} collection { id, title, stars }
 * @returns Collection movies above (>) 4 stars
 */
function getMoviesAboveFourStars(collection) {

}

module.exports = getMoviesAboveFourStars;
