const map = require('./ex3');

/**
 * @param {Array} collection { id, title, stars }
 * @returns Collection of ids and titles
 */
function getMoviesIdAndTitle(collection) {

}

module.exports = getMoviesIdAndTitle;
