const map = require('./ex7');

/**
 * @param {Array} collection { id, title, stars, views }
 * @returns Most viewed movie
 */
function getMostViewedMovie(collection) {

}

module.exports = getMostViewedMovie;
